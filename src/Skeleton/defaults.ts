import type { BoneAnimation, BoneColors } from '../Bone/BoneFeatures';

export const defaultColors: BoneColors = {
  background: '#E1E9EE',
  shimmer: '#F2F8FC',
};

export const defaultAnimation: BoneAnimation = {
  duration: 1500,
  direction: 'leftToRight',
  reverse: false,
};
